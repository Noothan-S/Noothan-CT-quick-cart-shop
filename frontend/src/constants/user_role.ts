interface IUserTRole {
  vendor: string;
  admin: string;
  customer: string;
}

const UserRole: IUserTRole = {
  vendor: "VENDOR",
  admin: "ADMIN",
  customer: "CUSTOMER",
};

export default UserRole;
